{
  "$note": "Deployment config. Copy this file to deployment.json and commit THAT in your private fork. The template ships only this example and never overwrites deployment.json (it is absent upstream), so your copy is sync-safe. tools/deployment_config.py is the single reader (Make + Python). Keys beginning with '$' are comments. See docs/extending.md.",
  "overlay": "_local"
}
