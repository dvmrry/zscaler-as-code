# pulls/

Fetcher output: `pulls/<tenant>/<resource_type>.json`, detail-shaped API
JSON consumed by `make transform`. Payloads are gitignored — they may
contain real tenant data at work and must never be committed. Only this
README and `.gitkeep` are tracked.
