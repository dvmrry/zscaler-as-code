# demo / zpa_microtenant_controller (generated env root)

Isolated Terraform root for `zpa_microtenant_controller` on tenant `demo`. GENERATED — do not
edit (AGENTS.md rule 6); regenerate with `make gen-env TENANT=demo`.
Config is loaded from `config/demo/zpa_microtenant_controller.auto.tfvars.json` at plan time.
