# demo / zcc_trusted_network (generated env root)

Isolated Terraform root for `zcc_trusted_network` on tenant `demo`. GENERATED — do not
edit (AGENTS.md rule 6); regenerate with `make gen-env TENANT=demo`.
Config is loaded from `config/demo/zcc_trusted_network.auto.tfvars.json` at plan time.
