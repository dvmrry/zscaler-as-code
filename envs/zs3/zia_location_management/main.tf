# GENERATED by tools/gen_env.py for tenant 'zs3' — do not edit.
# Regenerate: make gen-env TENANT=zs3

terraform {
  required_version = ">= 1.5"
  required_providers {
    zia = {
      source = "zscaler/zia"
    }
  }
  # local state — opt into remote state with
  # make gen-env TENANT=zs3 BACKEND=azurerm
}

provider "zia" {
  # credentials via env (ZSCALER_*/ZIA_*/ZPA_*); see tools/FETCH.md
}

variable "items" {
  # opaque at the root; the module enforces the strict type.
  type = any
}

module "zia_location_management" {
  source = "../../../modules/zia_location_management"
  items  = var.items
}
