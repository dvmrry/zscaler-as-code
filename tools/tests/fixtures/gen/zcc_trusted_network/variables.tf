# GENERATED by tools/gen_module.py from schemas/provider/zcc.json — do not edit.
# Regenerate: make generate

variable "items" {
  description = "zcc_trusted_network instances, keyed by a stable identifier."
  type = map(object({
    active                    = bool
    condition_type            = string
    dns_search_domains        = optional(list(string))
    dns_server_ips            = optional(list(string))
    hostname                  = optional(string)
    name                      = optional(string)
    resolved_ips_for_hostname = optional(list(string))
    ssid                      = optional(string)
    trusted_dhcp_servers_ips  = optional(list(string))
    trusted_egress_ips        = optional(list(string))
    trusted_gateway_ips       = optional(list(string))
    trusted_subnet_ips        = optional(list(string))
  }))
}
